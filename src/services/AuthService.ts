import { useSelector } from "react-redux"
import { LoggedSession } from "../slices/authSlice"

/**
 * Returns the session data from redux
 */
export const getSessionData = () => {
    // const session: LoggedSession = useSelector((state: LoggedSession) => state)
    // return session
}